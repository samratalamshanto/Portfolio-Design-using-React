/// <reference types="react" />
import { TypewriterProps } from '../Hooks/useTypewriter';
export interface ComponentProps extends TypewriterProps {
    cursor?: boolean;
    cursorStyle?: string;
}
export declare const Typewriter: ({ words, loop, typeSpeed, deleteSpeed, delaySpeed, onLoopDone, onType, cursor, cursorStyle }: ComponentProps) => JSX.Element;
