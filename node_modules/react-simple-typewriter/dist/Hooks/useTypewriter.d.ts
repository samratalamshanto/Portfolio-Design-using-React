export interface TypewriterProps {
    words: string[];
    loop?: number | boolean;
    typeSpeed?: number;
    deleteSpeed?: number;
    delaySpeed?: number;
    onLoopDone?: () => void;
    onType?: (counter: number) => void;
}
export declare const useTypewriter: ({ words, loop, typeSpeed, deleteSpeed, delaySpeed, onLoopDone, onType }: TypewriterProps) => {
    text: string;
    count: number;
};
